# Default print format
# can be an alias (singleLineString, multiLineString, json, yaml)...
defaultPrintFormat = singleLineString

# ...or a fully-qualified class name
#defaultPrintFormat = org.approvej.print.SingleLineStringPrintFormat


# Default file reviewer
# can be an alias ("none", "automatic", "script", or "ai")...
defaultFileReviewer = automatic

# ...or a fully-qualified class name
#defaultFileReviewer = org.approvej.review.AutomaticReviewer

# When defaultFileReviewer = script, set the script command
#reviewerScript = idea diff --wait "{receivedFile}" "{approvedFile}"

# When defaultFileReviewer = ai, set the AI command
#reviewerAiCommand = claude -p --allowedTools Read
