[id="reviewing"]
= Reviewing -- Check Differences

If the received value differs from the previously approved, ApproveJ will by default simply fail the test.
You then need to review the differences and decide if these are to be approved or actually were not intended.

Without a reviewer, you would have to locate the received and approved files yourself, open them side by side, and merge the changes manually.
A configured reviewer automates this: it opens your diff tool of choice directly on the right files, giving you a faster feedback loop and letting you approve changes without leaving the test run.


[id=blocking_review]
== Blocking/Non-Blocking Review

Some reviewers are blocking.
They trigger the diff/merge tool and wait for you to close it again.
This gives you the opportunity to merge the content of the two files before the test finishes, so the test does not fail due to your given approval.

Other implementations are non-blocking.
These simply display the differences between the two files, but will fail the test immediately.
So, after you merged the files or fixed the code, you need to run the test again.


[id=default_reviewer]
== Configure a Reviewer Script

The most common way to set up reviewing is to configure a review script in your `approvej.properties`.
Since each developer on a team may prefer a different diff tool, the best place for this is the user-level configuration file:

.`~/.config/approvej/approvej.properties`
[source,properties]
----
defaultFileReviewer = script
reviewerScript = idea diff --wait "{receivedFile}" "{approvedFile}"
----

The script can contain the following placeholders:

- `\{receivedFile}` — will be replaced with the path to the received file
- `\{approvedFile}` — will be replaced with the path to the approved file

TIP: Use `--wait` or similar flags if your diff tool supports them to make the review <<blocking_review,blocking>>.
This allows you to approve changes before the test finishes.

Here are configuration examples for common diff tools:

[source,properties]
.IntelliJ IDEA (blocking)
----
defaultFileReviewerScript = idea diff --wait "{receivedFile}" "{approvedFile}"
----

TIP: The <<intellij_plugin,ApproveJ IntelliJ plugin>> lets you view diffs and approve received files directly in the IDE — handy when you want to review changes without re-running the test to trigger the reviewer script.

[source,properties]
.VS Code (blocking)
----
defaultFileReviewerScript = code --wait --diff "{receivedFile}" "{approvedFile}"
----

[source,properties]
.Beyond Compare (blocking, macOS)
----
defaultFileReviewerScript = bcomp -wait "{receivedFile}" "{approvedFile}"
----

[source,properties]
.Meld (blocking)
----
defaultFileReviewerScript = meld "{receivedFile}" "{approvedFile}"
----

[source,properties]
.vimdiff (blocking)
----
defaultFileReviewerScript = vimdiff "{receivedFile}" "{approvedFile}"
----

You can also set this per project in `src/test/resources/approvej.properties` or via an environment variable (see <<configuration>>).


[id=single_test_reviewer]
== Override the Reviewer for a Single Test

In rare cases you may want to use a different reviewer for a specific test.
You can do so by calling the `reviewedBy` method on the link:{javadoc-url}/core/org/approvej/ApprovalBuilder.html[`ApprovalBuilder`].

[source,java,indent=0,role="primary"]
.Java
----
include::../../../test/java/examples/java/ReviewingDocTest.java[tag=approve_reviewedBy_fileReviewer]
----
[source,kotlin,indent=0,role="secondary"]
.Kotlin
----
include::../../../test/kotlin/examples/kotlin/ReviewingDocTest.kt[tag=approve_reviewedBy_fileReviewer]
----
<1> sets the given script to be executed to support the review
<2> executes the review script if the received value differs from the approved value


[id=automatic_review]
== Automatic Review

If you have a lot of approvals that need to be updated, you might want to just accept all the updates automatically.
In that case, you can use the link:{javadoc-url}/core/org/approvej/review/Reviewers.html#automatic()[`automatic`] link:{javadoc-url}/core/org/approvej/review/Reviewer.html[`Reviewer`].
It simply writes the received value into the approved file without checking them at all.

WARNING: This will overwrite your approved file(s).
You will probably want to check the changed files before committing them to version control!

[source,java,indent=0,role="primary"]
.Java
----
include::../../../test/java/examples/java/ReviewingDocTest.java[tag=approve_reviewedBy_automatic]
----
[source,kotlin,indent=0,role="secondary"]
.Kotlin
----
include::../../../test/kotlin/examples/kotlin/ReviewingDocTest.kt[tag=approve_reviewedBy_automatic]
----

You can also set this globally via configuration:

[source,properties]
----
defaultFileReviewer = automatic
----


[id=ai_review]
== AI-Assisted Review

You can configure an AI CLI tool to review approval test diffs automatically.
The AI evaluates whether changes look intentional and auto-approves if confident — otherwise the test fails for human review.

This is especially useful for image approvals, where multimodal AI can visually compare screenshots.

NOTE: AI-assisted review targets local development.
It requires an AI CLI tool (e.g., `claude`, `gemini`, `codex`) installed locally.

To enable AI-assisted review, set `defaultFileReviewer` to `ai` and configure the `reviewerAiCommand` property:

.`~/.config/approvej/approvej.properties`
[source,properties]
----
defaultFileReviewer = ai
reviewerAiCommand = claude -p --allowedTools Read
----

The command can contain `\{receivedFile}` and `\{approvedFile}` placeholders, which will be replaced with the actual file paths.

Here are configuration examples for common AI CLI tools:

[source,properties]
.Claude (with image support via Read tool)
----
aiReviewerCommand = claude -p --allowedTools Read
----

[source,properties]
.Gemini
----
aiReviewerCommand = gemini
----

[source,properties]
.Codex
----
aiReviewerCommand = codex
----

For **text files**, ApproveJ generates a unified diff and sends it to the AI as part of the prompt.
For **image files** (e.g., `.png`, `.jpg`), the AI is instructed to read the image files from disk.
The AI CLI needs permission to read files for image review to work (e.g., `--allowedTools Read` for Claude).

The AI is asked to respond with YES or NO on the first line.
If the AI responds with YES, the received file is automatically approved (<<blocking_review,blocking>>).
If it responds with NO or encounters an error, the test fails and a human developer needs to review the difference.

You can also override the AI reviewer for a single test:

[source,java,indent=0,role="primary"]
.Java
----
include::../../../test/java/examples/java/ReviewingDocTest.java[tag=approve_reviewedBy_ai]
----
[source,kotlin,indent=0,role="secondary"]
.Kotlin
----
include::../../../test/kotlin/examples/kotlin/ReviewingDocTest.kt[tag=approve_reviewedBy_ai]
----
<1> sets the AI CLI command to be used for review
<2> triggers the AI review if the received value differs from the approved value
