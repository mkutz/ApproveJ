spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/shop
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  sql:
    init:
      mode: always

payment:
  gateway:
    url: https://api.payment-provider.example.com
    token: default-token
